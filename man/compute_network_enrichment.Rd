% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_network_enrichment.R
\name{compute_network_enrichment}
\alias{compute_network_enrichment}
\title{Compute Z-Score and P-Value for Node Pagerank}
\usage{
compute_network_enrichment(node)
}
\arguments{
\item{node}{Either a named list / one-row data frame with \code{pagerank} and
permutation values whose names start with \code{"perm"} (e.g. \code{perm.1},
\code{perm.2}), or a data frame with one row per node in that layout, such as
the \code{pagerank_data} slot of a \code{\link[=run_tkoi]{run_tkoi()}} result with
\code{keep_permutations = TRUE}. A list of equal-length vectors is treated as
such a data frame. \code{node} needs a \code{pagerank} value and at least two
\verb{perm*} values, or an error is raised.}
}
\value{
A data frame with one row per node and columns:
\itemize{
\item \code{beta}: The computed z-score.
\item \code{p_value}: The one-tailed p-value derived from the z-score.
}
}
\description{
This function calculates the z-score and p-value for a node's pagerank
by comparing it to a set of permutation values. It returns a data frame
containing the z-score (\code{beta}) and p-value (\code{p_value}).
}
\details{
The function calculates the z-score as:
\deqn{z = \frac{\text{pagerank} -
\text{mean}(\text{perm_values})}{\text{sd}(\text{perm_values})}}
(\code{NaN} when the permutation values have no spread, as in
\code{\link{run_tkoi}}), and the p-value is calculated as the survival
function of the z-score:
\deqn{p = 1 - \Phi(z)}
where \eqn{\Phi} is the cumulative distribution function of the standard normal distribution.
}
\examples{
compute_network_enrichment(list(pagerank = 0.3, perm.1 = 0.1, perm.2 = 0.2, perm.3 = 0.15))

nodes = data.frame(pagerank = c(0.3, 0.1), perm.1 = c(0.1, 0.1), perm.2 = c(0.2, 0.12))
compute_network_enrichment(nodes)

}
